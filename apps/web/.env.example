# Mock services are restricted to non-production builds.
NEXT_PUBLIC_API_MODE=mock

# Required when NEXT_PUBLIC_API_MODE=http.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional development-only query inspection.
NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=false
