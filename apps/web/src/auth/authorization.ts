export { canEnterRoute, getRoutePolicy, ROUTE_POLICIES } from "./route-policy";

// Client-side policy improves navigation and messaging only. Every future API
// must authenticate the actor and enforce role, object and field scope itself.
